

<?php
$content = ob_get_clean();
require __DIR__ . '/layouts/main.phtml';
?>

<!-- catalog.phtml -->
<div class="container">
    <h3>Каталог товаров</h3>
    <div class="card-deck">
        <?php if (!empty($products)): ?>
            <?php foreach ($products as $product): ?>
                <div class="card text-center" style="width: 18rem;">
                    <img class="card-img-top" src="<?= $product['image_url']; ?>" alt="Product image" width="400" height="700">
                    <div class="card-body">
                        <h5 class="card-title"><?= $product['name']; ?></h5>
                        <p class="card-text"><?= $product['description']; ?></p>
                    </div>
                    <div class="card-footer">
                        <span class="price"><?= $product['price'] . ' руб'; ?></span>
                        <form action="/addProduct" method="POST" style="margin-top: 10px;">
                            <input type="hidden" name="product_id" value="<?= $product['id']; ?>">
                            <button type="submit" class="btn btn-primary">Добавить в корзину</button>
                        </form>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <p>Нет товаров в каталоге.</p>
        <?php endif; ?>
    </div>
</div>


<style>
    .card-deck {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: space-between;
    }

    .card {
        border: 1px solid #ddd;
        border-radius: 5px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        width: 18rem;
    }

    .card-img-top {
        width: 100%;
        height: 200px;
        object-fit: cover;
    }

    .card-body {
        padding: 15px;
    }

    .card-footer {
        padding: 10px;
        background-color: #f5f5f5;
        border-top: 1px solid #ddd;
    }

    .price {
        font-weight: bold;
        font-size: 1.1rem;
    }

    .btn-primary {
        background-color: #007bff;
        border: none;
        padding: 10px 20px;
        color: white;
        border-radius: 5px;
        cursor: pointer;
    }

    .btn-primary:hover {
        background-color: #0056b3;
    }
</style>



